export class Calculator {
    /**
     *
     * @param {string} string
     * @returns {number}
     */
    calculator(number) {
        //const changesArray = [{ 50: 10 }, { 20: 10 }, { 10: 10 }, { 5: 10 }, { 2: 10 }, { 1: 10 }]
        const changesArray = [10, 10, 10, 20, 20, 20]
        if (number > 50 && number % 50 === 0) {
            const result = number / 50
            for (let index = 2; index < array.length; index++) {
                const element = array[index]
            }
        }
    }
}
