
// import { fromEvent } from 'rxjs';
// import { map } from 'rxjs/operators'

// export const onClickCount = () => fromEvent(document, 'click').pipe(map((e: Event) => { x: window.screenX, 
//   y: window.screenY })
// }));
